# PHP DevOps Test Project

## Использование:
```bash
ansible-playbook -i inventory.yml playbook.yml
```
либо
```bash
ansible-playbook playbook.yml \
  -i <HOST>, \
  --private-key <KEY>
```
